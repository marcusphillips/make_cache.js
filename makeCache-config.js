LIB.configure('makeCache', {

  /* an object to place the makeCache maker function. if a string, it will be used to find an object from the global scope
   */
  // attachTo: <object or string>,


  /* duration, in miliseconds that makeCache will evict expired values (unless overridden at construction time)
   */
  // defaultEvictionInterval: <number>,

'':''});
